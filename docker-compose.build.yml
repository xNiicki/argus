# Local-dev override: build the narrator image from source instead of pulling
# it from GHCR. Use with:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
# or simply `make dev`.
services:
  narrator:
    build: ./narrator
